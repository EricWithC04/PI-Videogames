import React from 'react';

const SearchBar = () => {
    return (
        <form>
            <input type="text" placeholder='Game...'/>
            <button>Buscar</button>
        </form>
    );
};

export default SearchBar;